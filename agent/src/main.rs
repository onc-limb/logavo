// ASSUMPTION: Phase 0 of docs/spec.md only requires a buildable scaffold
// equivalent to `cargo new agent`; modules listed in the spec layout
// (tailer.rs, parser.rs, shipper.rs, proxy.rs) are introduced in later phases.
fn main() {
    println!("logavo agent");
}
